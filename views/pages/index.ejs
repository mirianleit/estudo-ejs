<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/head'); %>

  <body class="container">
    <%- include('../partials/header', {pagina: 'Home'}); %>
    <main>
      <div class="jumbotron">
        <h1>Node com EJS</h1>
        <p>Aprendendo a trabalhar com EJS</p>
        <p><%= subtitle %></p>
      </div>

      <ul>
        <% qualitys.forEach(function(quality){ %>
        <li><strong><%= quality.title %></strong> <%= quality.mensagem %></li>
        <% }) %>
      </ul>
    </main>

    <%- include('../partials/footer'); %>
  </body>
</html>
